services:
  company-dashboard-demo:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8788:8788"
    environment:
      DASHBOARD_PORT: 8788
      ORCHESTRATOR_ENABLED: 0
    restart: unless-stopped
